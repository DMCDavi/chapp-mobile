import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles(() => ({
    logo: {
        height: 200,
        marginBottom: 20,
        marginTop: 20
    },
    input: {
        marginBottom: 20
    },
    button: {
        marginBottom: 20
    },
    formControl: {
        paddingRight: 20,
        paddingLeft: 20
    }
}));

export default useStyles;